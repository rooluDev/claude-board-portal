spring:
  application:
    name: admin-page

  # Thymeleaf 설정
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

  # 데이터소스 설정
  datasource:
    url: jdbc:mysql://localhost:3306/potal?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: asd@1252370
    driver-class-name: com.mysql.cj.jdbc.Driver

  # 파일 업로드 설정
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 10MB
      file-size-threshold: 1MB

# MyBatis 설정
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.ebrain.admin.dto
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 30

# 서버 설정
server:
  port: 8082
  servlet:
    context-path: /
    session:
      timeout: 30m
  error:
    whitelabel:
      enabled: false

# 파일 저장 경로
file:
  upload:
    base-path: /Users/user/upload  # 개발 환경
    free: ${file.upload.base-path}/free
    gallery: ${file.upload.base-path}/gallery
    thumbnail: ${file.upload.base-path}/thumbnail

# 로깅 설정
logging:
  level:
    com.ebrain.admin: DEBUG
    org.springframework.web: INFO
    org.mybatis: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
